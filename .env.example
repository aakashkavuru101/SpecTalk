# Optional — only needed if you want a server-side Anthropic fallback.
# With BYOK, users bring their own keys via the in-app provider settings.
# Leave this empty for a fully BYOK deployment (recommended for Vercel).
ANTHROPIC_API_KEY=
